// The one evidence-tier vocabulary (idea-atlas §3.1). The Reality ledger, the
// landmark panels and the Blender export comments all read this instead of
// each inventing their own three words for "how sure are we of this number".
export type EvidenceTier = "measured" | "declared" | "unmeasured";
